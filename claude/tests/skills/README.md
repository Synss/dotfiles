# Skill tests

One suite per skill, under `suites/`, sharing helpers from `lib/` and
fixtures from `fixtures/<suite>/`. `run.sh` dispatches to a suite; a
suite script also runs standalone.

Run without arguments, or with an unrecognized one, for usage.
