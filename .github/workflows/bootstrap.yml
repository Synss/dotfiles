name: bootstrap

on:
  pull_request:
    branches: [main]
    paths:
      - bin/bootstrap.sh
      - .github/workflows/bootstrap.yml
  schedule:
    # First day of every month at 2:00 AM UTC.
    - cron: '0 2 1 * *'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            host: github-actions-macos
          - os: ubuntu-latest
            host: github-actions-linux
    name: bootstrap on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Bootstrap
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./bin/bootstrap.sh ${{ matrix.host }} 2> bootstrap.err | tee bootstrap.log || true
          grep -q 'Nix was installed successfully!' bootstrap.log || exit 1
      - uses: actions/upload-artifact@v7
        if: '!cancelled()'
        with:
          name: Upload bootstrap.err
          path: bootstrap.err
          if-no-files-found: error
          retention-days: 8
      - uses: DeterminateSystems/magic-nix-cache-action@main
        with:
          use-flakehub: false
      - name: Smoke test for nix
        run: nix --version
      - name: Smake test for jj
        run: jj status
      - name: Test bootstrap idempotency
        run: ./bin/bootstrap.sh ${{ matrix.host }}
