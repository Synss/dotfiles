# zsh configuration

`.zshrc` and `.zshenv` are generated by Home Manager (`programs.zsh` in
`../nix/home.nix`), which sources `conf.d/*.zsh` in filename order, then
`conf.d/*.local` for machine-local overrides, gitignored.

See [conf.d/README.md](conf.d/README.md) for the numbering scheme.
