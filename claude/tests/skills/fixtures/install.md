# Installing the tool

Installation of the package is performed by the administrator through
the use of the package manager.

The daemon reads its configuration at startup; the configuration file
lives in /etc/tool.conf and is reloaded on SIGHUP.

One caveat applies to every command below — none of them work without
root.
