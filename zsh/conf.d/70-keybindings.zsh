# History navigation
bindkey "^P" history-incremental-search-backward
bindkey "^N" history-incremental-search-forward

# zsh-autosuggestions: Ctrl-f accepts full suggestion
# Alt-f / Esc+W step through word by word (vi-forward-word, default partial-accept)
bindkey "^f" autosuggest-accept
