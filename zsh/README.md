# Zsh configuration

`.zshrc` and `.zshenv` are generated by Home Manager. `programs.zsh` in
`../nix/home.nix` sources `conf.d/*.zsh` in filename order, then
`conf.d/*.local`, which are gitignored, for machine-local overrides.

See [`conf.d/README.md`](conf.d/README.md) for the numbering scheme.
